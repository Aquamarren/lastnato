<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marrenmatias.trynavdrawer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SetIncome"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddCategory" />
        <activity android:name=".AddSavings" />
        <activity android:name=".ForecastBudget" />
        <activity android:name=".Main4Activity" />
        <activity android:name=".MainActivity"/>

        <activity android:name=".SetBudget_first" />
        <activity android:name=".SetSavings" />
        <activity android:name=".UseSavings"></activity>
    </application>

</manifest>